public class AlphabetGenerator {
    /**
     * Given a numeric base, return a char[] that maps every digit that is
     * representable in that base to a lower-case char.
     * 
     * Weight each character of the alphabet proportional to its occurrence
     * in words in "trainingData".
     * 
     * Example:
     *   trainingData = {"aa", "bbc"};
     * In input, Pr(a) = 2/5, Pr(b) = 2/5, Pr(c) = 1/5.
     * In the output for base 10, they should be in the same proportion.
     *   base = 10
     *   expectedOutput = {'a', 'a', 'a', 'a',
     *                     'b', 'b', 'b', 'b',
     *                     'c', 'c'};
     */
    public static char[] generateFrequencyAlphabet(int base,
                                                   String[] trainingData) {
        return null; // REQ: please implement
    }
}
